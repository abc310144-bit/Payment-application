# 付款申請系統產品需求規格（PRD）

| 項目 | 說明 |
| --- | --- |
| 位置 | 財務 → 付款申請 → 一般付款 |
| 角色 | 建檔人、財務、出納（原型可切換） |
| 文件性質 | 依現行 DEMO／已確認需求整理，作為驗收與後續開發／後端對接依據 |
| 版本對應 | 現行前端：建檔 1→2→3、檢視無步驟、核銷獨立頁、出納付款 |

---

## 文件導讀

本 PRD 依**功能模組**說明；每個欄位／按鈕盡量標明：

- **定義**：使用者看到的意義  
- **資料來源**：從哪裡來（使用者輸入／系統帶入／計算）  
- **計算公式**：若有  
- **頁面交互**：何時顯示、必填、跳轉、權限  

---

# 〇、共通：角色、狀態、金額規則

## 0.1 角色定義

| 角色 | 定位 |
| --- | --- |
| 建檔人 | 建立／編輯申請（草稿、審核不通過）、導出送審、預付單完成付款後**進行核銷** |
| 財務 | 審核、多數狀態可編輯（核銷階段除外）、作廢；可檢視核銷歷史，**不可回壓** |
| 出納 | 僅查詢、檢視、勾選、**完成付款／付款失敗**（含批量）；不可新增、編輯、作廢、核銷 |

原型建檔人帳號：`ruby.lee`。

## 0.2 母單狀態（PaymentStatus）

`草稿`｜`待審核`｜`審核不通過`｜`待付款`｜`付款失敗`｜`待核銷`｜`部分核銷`｜`已完成`｜`已作廢`

### 狀態轉換總表

| 觸發 | 母單變更 | 明細連動 | 其他 |
| --- | --- | --- | --- |
| 建立申請 | → `草稿` | vouchers 空 | 單號 `PA{YYYYMMDD}{序號3位}` |
| 導出送線下審核 | `草稿`／`審核不通過` → `待審核` | 該批 `草稿` 明細 → `待審核` | 原審核不通過則清空不通過原因 |
| 財務審核通過 | → `待付款`；**通路費用** → `已完成` | `待審核` → `審核通過`；通路 → `已完成` | 回壓預計付款日（依規則） |
| 財務審核不通過 | → `審核不通過` | `待審核`／`審核通過` → `草稿` | 必填不通過原因（覆蓋） |
| 出納完成付款 | 非預付 → `已完成`；預付 → `待核銷` | `審核通過` → `已完成` 或 `待核銷` | 寫入實際付款日；外幣寫入付款匯率 |
| 出納付款失敗 | `待付款` → `付款失敗` | 不變 | — |
| 核銷回壓／作廢回壓 | `待核銷`／`部分核銷`／`已完成` 依餘額重算 | 見核銷章節 | — |
| 作廢（列表） | → `已作廢` | 不刪明細列狀態語意；UI 僅檢視 | **後端：DB 不刪資料，僅狀態作廢** |

> 母單與明細非完全一對一同步；各動作只改對應階段的明細列。

## 0.3 明細狀態（VoucherDetailStatus）

`草稿`｜`待審核`｜`審核通過`｜`待核銷`｜`部分核銷`｜`核銷完成`｜`已完成`

| 觸發 | 明細變更 |
| --- | --- |
| 新增明細 | → `草稿` |
| 導出 | `草稿` → `待審核` |
| 審核通過 | `待審核` → `審核通過`（通路 → `已完成`） |
| 審核不通過 | `待審核`／`審核通過` → `草稿` |
| 完成付款 | `審核通過` → `已完成`；預付 → `待核銷` |
| 回壓核銷 | `待核銷` ↔ `部分核銷` ↔ `核銷完成`（見核銷公式） |

## 0.4 金額與幣別

| 項目 | 臺幣 TWD | 外幣（非臺幣） |
| --- | --- | --- |
| 輸入 | 正整數（不可 0） | ≥ `0.001`，最多小數 **3** 位 |
| 加總／餘額 | 整數規則／`roundMoney`（三位） | 先四捨五入到第 3 位再比 |
| 列表總金額顯示 | `NT$` + 千分位 | 純數字（最多 3 位小數） |
| 完成付款匯率 | 不需 | 必填；>0；最多 6 位小數 |

**母單總付款金額（含稅）來源**

- URMART 月結：月結帶入之「貴公司開立發票金額(含稅)」  
- 其餘：明細「付款金額」加總（`sumAmounts`）

> 步驟 1 畫面**不顯示**總付款金額欄；僅檢視／步驟 3／列表顯示。

## 0.5 付款類型對照

| 申請款項類型 | 規則類別 | 結算／對帳月 | 憑證明細 | 審核後 | 事後核銷 |
| --- | --- | --- | --- | --- | --- |
| 個人代墊報支 | 零用金 | 無 | 一般明細 | → 待付款 | 否 |
| 廠商實報實銷 | 一般 | 無 | 一般明細 | → 待付款 | 否 |
| 廠商預付 / 訂金 | 預付款 | 無 | 憑證樣式僅合約／報價單 | → 待付款；付款後 → 待核銷 | **是** |
| 通路費用 (通路後扣) | 待確認 | **對帳月份**（必填） | 僅發票（invoice-only） | **直接已完成**（無待付款） | 否 |
| URMART 月結廠商 | 待確認 | **結算月**（必填） | 僅發票；幣別鎖臺幣 | → 待付款 | 否 |

---

# 一、一般付款（列表）

功能：查詢付款申請、依角色操作、進入新增／編輯／檢視／核銷／批量付款。

## 1.1 篩選條件

| 欄位 | 控件 | 定義 | 資料來源／匹配 | 交互 |
| --- | --- | --- | --- | --- |
| 申請單號 | 文字 | 模糊查單號 | 即時：`fuzzyMatch`（不分大小寫；忽略空白、`-`、`_`；多關鍵字需全部命中） | 輸入即過濾，不必先按查詢 |
| 申請款項類型 | 下拉 | 五種付款類型 | 空＝全部；否則精確等於 | 需按「查詢」套用 |
| 狀態 | 下拉 | 母單狀態 | 空＝全部；否則精確等於 | 需查詢 |
| 申請人 | 文字 | 申請人帳號／名稱 | `applicant` 子字串（trim 後 includes） | 需查詢 |
| 付款幣別 | 下拉 | overview 幣別 | 無 overview 視同 `臺幣TWD` | 需查詢 |
| 付款對象 | radio＋下拉 | 廠商或員工 | radio 切換清空已選；**僅選到具體對象**才依 `overview.vendorId` 過濾；只選種類不過濾 | 需查詢 |
| 建立日 | 日期區間 | 建單日 | 取日期前 10 碼；雙端閉區間；單端＝該日等於 | 需查詢 |
| 預計付款日 | 日期區間 | 預計付款日 | 同上；空值＋有條件 → 不通過 | 需查詢 |
| 實際付款日 | 日期區間 | 實際付款日 | 同上 | 需查詢 |

### 篩選區按鈕

| 按鈕 | 行為 |
| --- | --- |
| 查詢 | 將目前條件套用為 applied 條件（申請單號除外已即時） |
| 重置 | 清空條件、勾選、提示訊息 |
| 新增付款申請 | 跳至「新增」步驟 1；**出納 disabled** |

## 1.2 篩選結果（欄位）

| 欄位 | 定義 | 資料來源 |
| --- | --- | --- |
| （勾選） | 批量付款用 | 本頁勾選 |
| 申請單號 | 系統產生 | `applicationNo` |
| 申請款項類型 | 五種之一 | `paymentType` |
| 付款對象 | 顯示名 | `overview.vendorName` 或依 vendorId 對照 |
| 總金額 | 總付款金額（含稅） | 見 §0.4；`formatMoney` |
| 付款幣別 | — | `overview.currency` |
| 付款匯率 | 完成付款後回壓 | `paymentExchangeRate`；無則 `-` |
| 建立時間 | 建單時間 | `createdAt` |
| 預計付款日 | — | `expectedPaymentDate`；空 `-` |
| 實際付款日 | — | `actualPaymentDate`；空 `-` |
| 不通過原因 | 審核不通過 | `rejectReason`；超過約 20 字省略，懸浮全文 |
| 狀態 | 母單狀態徽章 | `status` |
| 操作 | 依角色×狀態 | 見下節 |

空結果文案：`查無符合條件的付款申請`；頁尾：`共 N 筆`。

## 1.3 列操作按鈕

固定可能出現：`編輯`｜`進行核銷`｜`檢視`｜`作廢`。

### 一般狀態（非「預付＋待核銷／部分核銷」）

顯示：`編輯`、`檢視`、`作廢`（不可用則 **disabled 置灰**）。

| 狀態 | 建檔人 編輯／作廢 | 財務 編輯／作廢 | 出納 |
| --- | --- | --- | --- |
| 草稿、審核不通過 | ✓／✓ | ✓／✓ | 編輯✗ 作廢✗；檢視✓ |
| 待審核、待付款 | ✗／✗ | ✓／✓ | 同上 |
| 付款失敗 | ✗／✗ | ✗／✓ | 同上 |
| 已完成、已作廢 | ✗／✗ | ✗／✗ | 同上 |

### 預付核銷階段（`廠商預付 / 訂金` 且 `待核銷`｜`部分核銷`）

「編輯」改為「**進行核銷**」：

| 按鈕 | 建檔人 | 財務／出納 |
| --- | --- | --- |
| 進行核銷 | ✓ 可點 | 顯示但 **置灰** |
| 檢視 | ✓ | ✓ |
| 作廢 | 置灰 | 財務✓；出納置灰 |

### 按鈕行為

| 按鈕 | 跳轉／行為 |
| --- | --- |
| 編輯 | `/applications/:id/overview`（步驟 1，含 stepper） |
| 檢視 | `/applications/:id/view`（**無** 1→2→3；付款申請詳情） |
| 進行核銷 | `/applications/:id/writeoff`（核銷作業頁） |
| 作廢 | 確認後母單 → `已作廢`；**後端 DB 不刪該申請資料** |

## 1.4 批量操作（結果上方）

| 按鈕 | 顯示條件 | 行為 |
| --- | --- | --- |
| 批量完成付款 | 出納且勾選列皆可完成付款 | 外幣需填匯率；幣別須全部相同否則 alert |
| 批量付款失敗 | 出納且勾選列皆為 `待付款`（且非通路費用） | 標記付款失敗 |

---

# 二、新增／編輯付款申請（1→2→3）

路徑概念：

1. **建立基本資料**（`overview`／新增頁）  
2. **設定款項憑證明細**（`details`）  
3. **付款申請總覽**（`summary`）  

頁面共通：

- 頂部置中 stepper；右上「返回列表」  
- 步驟底右並排：`返回`｜主按鈕（儲存／導出等）  
- 步驟 1「返回」→ 列表；步驟 2「返回」→ 步驟 1；步驟 3「返回」→ 步驟 2  

可編輯條件：`canEditApplication`（出納永遠唯讀；核銷階段不可編輯申請內容）。

---

## 2.1 步驟一：建立基本資料

### 欄位定義

| 欄位 | 必填 | 顯示條件 | 資料來源／預設 | 驗證與交互 |
| --- | --- | --- | --- | --- |
| 付款類型 | ✓ | 常駐 | 下拉五種；預設個人代墊 | 切換：清結算／對帳月、UM 相關、重算預計付款日；代墊→鎖匯款＋員工對象 |
| 結算月／對帳月份 | ✓（顯示時） | `needsSettlementMonth` | `type=month` | **通路費用**標籤＝「對帳月份」；**URMART**＝「結算月」 |
| 申請人 | ✓ | 常駐 | 系統 `CURRENT_USER_ACCOUNT` | 唯讀 disabled |
| 申請日 | ✓ | 常駐 | 預設今日 | 變更重算預計付款日 |
| 付款對象 | ✓ | 常駐 | 代墊＝員工列表；其餘＝廠商 | VendorSelect／員工選 |
| 付款幣別 | ✓ | 常駐 | 預設臺幣；URMART **鎖定臺幣** | — |
| 付款方式 | ✓ | 常駐 | 預設匯款；代墊僅「匯款」 | — |
| 匯款手續費 | ✓ | 常駐 | 公司負擔／收款人負擔 | — |
| 預計付款日 | ✓（非通路） | 非通路費用 | 系統依規則計算 | 見下「預計付款日規則」；通路**不顯示** |

### 預計付款日規則

| 類型 | 計算 | 可否手改 |
| --- | --- | --- |
| 個人代墊（零用金） | 下一個曆週的週四 | 否 |
| 廠商實報實銷（一般） | 申請日 21～次月 5 → **次月 15**；6～20 → **當月 30**（無 30 則月末） | 否 |
| 廠商預付 | 預設＝申請日；允許範圍申請日～申請日+5（含） | 是（限範圍內） |
| URMART 月結 | 申請日 1–25：預設當月 25，可改次月 25；≥26：僅次月 25 | 部分可（≤25 日可兩選） |
| 通路費用 | 不適用 | 欄位不出現 |

### URMART 月結額外帶入

選完廠商＋結算月後（DEMO）：`lookupMockUmSettlement` → `monthlyTotals`、總額＝貴公司開立發票金額、統編等。正式環境改資料庫查詢。

### 按鈕

| 按鈕 | 行為 |
| --- | --- |
| 儲存此分頁 | 通過驗證後：新增則 `createApplication`（草稿）並進步驟 2；編輯則 `updateOverview` 並進步驟 2 |
| 返回 | 回列表 |

---

## 2.2 步驟二：設定款項憑證明細

### 工具列

| 元素 | 定義／交互 |
| --- | --- |
| 總筆數／每頁筆數 | 10／20／50 |
| + 新增明細／+ 新增發票 | `canAddVoucherDetail`：建檔人草稿／審核不通過；財務另含待審核。母單已完成／已作廢／付款失敗不可新增 |
| （無導出／審核） | 已移至步驟 3 |

### 明細表欄位

| 欄位 | 資料來源 |
| --- | --- |
| ID | 列序（1-based） |
| 廠商統編 | 明細 `vendorTaxId` |
| 款項用途 | `purpose`（下拉不含「URMART月結廠商」；月結／通路 invoice-only 由系統帶入） |
| 備註單號 | 依用途第二欄組成（invoice-only 不顯示此欄） |
| 憑證樣式 | 預付僅合約／報價單；一般見樣式表；invoice-only 固定發票 |
| 發票格式／號碼／日期 | 樣式為發票時 |
| 是否應稅 | `應稅`／`零稅率`／`免稅`；未選顯示 `-` |
| 未稅金額／稅額／付款金額 | 見稅額公式 |
| 付款匯率 | 僅外幣母單；完成付款後回壓 |
| 狀態 | 明細狀態 |
| 操作 | 檢視／編輯／作廢（權限見下） |

**明細列操作**

- 草稿：建檔人／財務可檢視、編輯、作廢  
- 導出後：財務可編輯／作廢待審核與審核通過列；出納僅檢視  
- 母單已完成／已作廢／付款失敗：僅檢視  

明細「作廢」＝自申請移除該明細（前端 remove）；與母單作廢不同。

### 新增／編輯明細彈窗（重點欄位）

| 欄位 | 必填規則 | 說明 |
| --- | --- | --- |
| 款項用途 | 非 invoice-only 必填 | 選項見用途表；**無 URMART月結廠商** |
| 第二欄（PO／結算月／對帳月份／期間等） | 依用途 | 通路用途第二欄標籤＝**對帳月份** |
| 憑證樣式 | 非 invoice-only 必填 | — |
| 發票格式／日期 | 樣式＝發票時必填 | invoice-only 視同發票 |
| 發票號碼(憑證號碼) | 必填 | — |
| 廠商統編 | 8 位數字 | 代墊可空預設；UM 帶 overview 統編 |
| **是否應稅** | **樣式＝發票（含 invoice-only）必填**；其餘非必填 | 請選擇／應稅／零稅率／免稅；外幣新建預設請選擇 |
| 明細細項名稱／金額 | 每列必填 | **細項無是否應稅**；稅別跟主檔 |
| 憑證檔案 | 必填 | 上傳 |
| 附件 | 選填 | — |

#### 稅額計算公式（`TAX_RATE = 1.05`）

對每一細項金額 `amt`（臺幣整數四捨五入；外幣三位小數 round）：

1. **應稅**（金額視為含稅付款額）  
   - `pay = round(amt)`  
   - `untaxed = round(amt / 1.05)`  
   - `tax = round(pay - untaxed)`  

2. **零稅率／免稅／未選**（比照舊「未稅」）  
   - `pay = round(amt)`  
   - `untaxed = pay`  
   - `tax = 0`  

明細合計：各行 `untaxed`／`pay` 加總後再 round；`taxAmount = round(pay合計 - untaxed合計)`。

### URMART 月結額外規則

- 顯示月結彙總（唯讀）  
- 導出前：目前發票總金額與「貴公司開立發票金額」相差須在 **±3 元**（含）內  

### 步驟底按鈕

| 按鈕 | 行為 |
| --- | --- |
| 返回 | → 步驟 1 |
| 儲存此分頁 | → 步驟 3（明細已於彈窗個別儲存） |

---

## 2.3 步驟三：付款申請總覽

### 基本資料區（唯讀）

申請單號、狀態、付款類型、申請人、申請日、付款對象、統編（有則顯示）、結算月或對帳月份（有則顯示）、幣別、付款方式、匯費、預計／實際付款日、付款匯率、**總付款金額（含稅）**、不通過原因（有則顯示）。

### 款項憑證明細區

欄位與步驟 2 相同；操作僅「**檢視**」（開憑證／附件預覽，與步驟 2 檢視相同）。

### 核銷歷史區（條件顯示）

- 條件：付款類型需事後核銷 **且** 已有 `actualPaymentDate`  
- 內容：回壓紀錄列表、檢視憑證；**不可回壓**（回壓僅在「進行核銷」頁）  
- 尚無回壓：顯示尚無核銷回壓紀錄  

### 底部按鈕（依角色）

| 角色／條件 | 按鈕 | 行為 |
| --- | --- | --- |
| 非出納 | 導出送線下審核 | 產 PDF；首次將草稿明細→待審核、母單→待審核；可重複下載已導出檔 |
| 財務＋待審核 | 審核通過／審核不通過 | 通過→待付款（通路→已完成）；不通過必填原因→審核不通過 |
| 出納＋可付款 | 完成付款 | 見「出納付款流程」 |
| 出納＋待付款 | 付款失敗 | 母單→付款失敗 |
| 步驟內 | 返回 | → 步驟 2 |

**導出可用性**：有明細、母單非已完成／已作廢／付款失敗、存在草稿或已有導出檔、出納不可、UM 需通過 ±3 檢核。

---

# 三、檢視（無步驟）

入口：列表「檢視」→ `/applications/:id/view`。

| 項目 | 說明 |
| --- | --- |
| 版面 | **不顯示** 1→2→3；標題「付款申請詳情」；右上「返回列表」 |
| 內容 | 與步驟 3 相同元件（基本資料＋明細＋條件核銷歷史＋角色按鈕） |
| 差異 | 無步驟底「返回」；僅「返回列表」 |

---

# 四、進行核銷流程

適用：**廠商預付 / 訂金**，母單 `待核銷` 或 `部分核銷`。

## 4.1 入口

列表「進行核銷」（建檔人可點；他人置灰）→ `/applications/:id/writeoff`。

無權限或不符狀態 → 導向檢視頁。

## 4.2 核銷作業頁

| 元素 | 說明 |
| --- | --- |
| 標題 | 進行核銷；顯示單號、狀態 |
| 返回 | **僅「返回列表」** |
| 摘要 | 母單未核銷餘額 |
| 明細列 | 用途、是否應稅、金額、「回壓憑證文件」 |
| 子列 | 已回壓紀錄：標籤、檢視憑證、作廢（僅建檔人可） |

### 回壓規則（與現行一致）

- 回壓金額 ≤ 該明細未核銷餘額；臺幣 ≥1、外幣 ≥0.001  
- 樣式：發票／其他；發票需號碼、日期、格式等  
- 特定廠商（如龍鋒報關）需發票統編  

### 明細核銷狀態公式

設 `written`＝該明細回壓金額加總，`pay`＝明細付款金額：

- `written ≤ 0` → `待核銷`  
- `written < pay` → `部分核銷`  
- 否則 → `核銷完成`  

### 母單核銷狀態公式（僅統計核銷階段明細）

- 全部 `核銷完成` → 母單 `已完成`  
- 全部 `待核銷` → 母單 `待核銷`  
- 其餘 → `部分核銷`  

### 後端資料（待實作注意）

| 操作 | DB 行為 |
| --- | --- |
| 列表作廢申請單 | **不刪**申請資料，僅狀態作廢 |
| 核銷紀錄「作廢」 | **刪除**該筆回壓／核銷資料 |

---

# 五、出納付款流程

## 5.1 單筆（檢視或步驟 3）

| 按鈕 | 條件 | 結果 |
| --- | --- | --- |
| 完成付款 | 出納；狀態 `待付款` 或 `付款失敗`；非「通路費用」（通路審核即完成） | 寫入實際付款日；外幣必填匯率；預付→`待核銷`＋明細待核銷；否則→`已完成` |
| 付款失敗 | 出納；僅 `待付款`；非通路 | 母單→`付款失敗` |

## 5.2 批量（列表）

- 批量完成付款／批量付款失敗：條件同上且勾選列皆符合  
- 批量完成：勾選列幣別須相同  

完成預付後提示：建檔人可於列表「進行核銷」；歷史可於「檢視」查看。

---

# 六、頁面路由一覽

| 路徑 | 說明 |
| --- | --- |
| `/` | 一般付款列表 |
| `/overview` | 新增步驟 1 |
| `/applications/:id/overview` | 編輯步驟 1 |
| `/applications/:id/details` | 步驟 2 |
| `/applications/:id/summary` | 步驟 3（含 stepper） |
| `/applications/:id/view` | 檢視詳情（無 stepper） |
| `/applications/:id/writeoff` | 進行核銷作業頁 |

---

# 七、驗收檢查清單（摘要）

- [ ] 三角色列表操作／置灰符合 §1.3  
- [ ] 新增／編輯走 1→2→3；檢視無步驟  
- [ ] 通路「對帳月份」、URMART「結算月」；新增明細用途無「URMART月結廠商」  
- [ ] 是否應稅三值＋細項跟主檔；稅額公式符合 §2.2  
- [ ] 導出／審核在步驟 3 與檢視；出納僅付款鈕  
- [ ] 預付付款後建檔人可進行核銷；檢視可見核銷歷史  
- [ ] 作廢申請不刪庫；作廢核銷紀錄刪庫（後端）  

---

*本文件依現行 DEMO 行為撰寫；後端對接時請以本章狀態機與「後端資料」小節為準，並依實作調整 API 契約。*
